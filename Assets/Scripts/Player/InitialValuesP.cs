using Unity.VisualScripting;
using UnityEngine;

public class InitialValuesP : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    PlayerAttributes player; //reference another script in a script

    void Start()
    {

    }

}
